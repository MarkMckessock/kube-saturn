endpoints:
  - name: "${APP}"
    group: internal
    url: "http://${GATUS_SERVICE:=${APP}}.${GATUS_NAMESPACE}.svc.cluster.local:${GATUS_PORT:=80}${GATUS_PATH:=/}"
    interval: 1m
    conditions:
      - "[STATUS] == ${GATUS_STATUS:=200}"
