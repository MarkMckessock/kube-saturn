apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gotify
  namespace: gotify
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
