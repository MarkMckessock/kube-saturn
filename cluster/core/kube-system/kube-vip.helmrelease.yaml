apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: kube-vip
  namespace: kube-system
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://kube-vip.io/helm-charts
      chart: kube-vip
      version: 0.4.2
      sourceRef:
        kind: HelmRepository
        name: kube-vip
        namespace: flux-system
      interval: 5m
  values:
    config: ${CONTROL_PLANE_IP}