apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-vol
  namespace: minio
spec:
  volumeName: backup-vol
  accessModes:
  - ReadWriteMany      
  resources:
     requests:
       storage: 200Gi 
  storageClassName: ""