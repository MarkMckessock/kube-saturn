apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ubooquity
  namespace: ubooquity
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
