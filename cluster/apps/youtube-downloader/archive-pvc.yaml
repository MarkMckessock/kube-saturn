apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: youtube-dl-archive
  namespace: youtube-dl
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}