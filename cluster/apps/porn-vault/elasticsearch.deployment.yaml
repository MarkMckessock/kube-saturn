apiVersion: apps/v1
kind: Deployment
metadata:
  name: elasticsearch
  namespace: porn-vault
spec:
  selector:
    matchLabels:
      app: elasticsearch
  template:
    metadata:
      labels:
        app: elasticsearch
    spec:
      containers:
        - name: elasticsearch
          image: docker.elastic.co/elasticsearch/elasticsearch:8.1.0
          ports:
            - name: elastic-9200
              containerPort: 9200
            - name: elastic-9300
              containerPort: 9300
          env:
            - name: discovery.type
              value: single-node
          resources:
            limits:
              memory: "4Gi"
              cpu: "4.0"
