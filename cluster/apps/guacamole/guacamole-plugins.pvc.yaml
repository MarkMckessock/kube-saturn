apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: guacamole-plugins
  namespace: guacamole
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: ${STORAGE_CLASS}