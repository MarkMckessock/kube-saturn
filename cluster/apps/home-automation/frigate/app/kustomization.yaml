apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: frigate
resources:
  - ./helmrelease.yaml
  - ./namespace.yaml
configMapGenerator:
  - name: frigate-configmap
    files:
      - config/config.yaml
generatorOptions:
  disableNameSuffixHash: true