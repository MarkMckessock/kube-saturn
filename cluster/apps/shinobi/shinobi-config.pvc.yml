apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shinobi-config
  namespace: shinobi
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
