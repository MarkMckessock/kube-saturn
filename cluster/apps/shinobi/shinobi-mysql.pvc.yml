apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: shinobi-mysql
  namespace: shinobi
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
