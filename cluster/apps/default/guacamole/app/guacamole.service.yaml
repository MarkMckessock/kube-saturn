apiVersion: v1
kind: Service
metadata:
  name: guacamole
  namespace: guacamole
spec:
  selector:
    app.kubernetes.io/name: guacamole
    app.kubernetes.io/component: guacamole
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
    
