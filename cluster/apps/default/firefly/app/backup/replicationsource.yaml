---
apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: firefly
spec:
  sourcePVC: firefly-firefly-upload
  trigger:
    schedule: "0 0 * * *"
  restic:
    copyMethod: Snapshot
    pruneIntervalDays: 10
    repository: firefly-restic-secret
    cacheCapacity: 2Gi
    volumeSnapshotClassName: csi-ceph-blockpool
    storageClassName: ceph-block
    retain:
      daily: 10
      within: 3d
