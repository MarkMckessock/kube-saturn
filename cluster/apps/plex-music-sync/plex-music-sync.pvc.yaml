apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-media-sync
  namespace: plex-media-sync
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
