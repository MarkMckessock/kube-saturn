apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ha-postgres
  namespace: home-assistant
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
  storageClassName: truenas-iscsi-csi
