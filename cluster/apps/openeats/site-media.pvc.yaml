apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: site-media
  namespace: openeats
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-nfs-csi
