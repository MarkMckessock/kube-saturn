apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openeats-static-files
  namespace: openeats
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}