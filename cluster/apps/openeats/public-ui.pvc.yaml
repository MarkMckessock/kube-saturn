apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: public-ui
  namespace: openeats
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-nfs-csi
