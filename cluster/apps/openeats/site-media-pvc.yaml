apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openeats-site-media
  namespace: openeats
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}