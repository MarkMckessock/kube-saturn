apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard
  namespace: adguard
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: truenas-iscsi-csi
