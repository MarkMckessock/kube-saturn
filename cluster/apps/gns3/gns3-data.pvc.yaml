apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: gns3
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-nfs-csi
