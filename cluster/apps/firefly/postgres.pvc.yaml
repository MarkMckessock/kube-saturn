apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres
  namespace: firefly-iii
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: truenas-iscsi-csi
