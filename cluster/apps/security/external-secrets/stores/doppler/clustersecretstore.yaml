apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-mosquitto
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: mosquitto
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-home-assistant
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: home-assistant
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-frigate
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: frigate
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-zigbee2mqtt
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: zigbee2mqtt
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-cloudnative-pg
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: cloudnative-pg
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-minio
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: minio
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-thanos
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: thanos
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-cloudflare-ddns
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            namespace: security
            name: doppler-tokens
            key: cloudflare-ddns