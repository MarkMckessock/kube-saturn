apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-mosquitto
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-tokens
            key: mosquitto
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-home-assistant
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-tokens
            key: home-assistant
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-frigate
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-tokens
            key: frigate
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: doppler-zigbee2mqtt
  namespace: security
spec:
  provider:
    doppler:
      auth:
        secretRef:
          dopplerToken:
            name: doppler-tokens
            key: zigbee2mqtt