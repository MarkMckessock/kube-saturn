apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-config
  namespace: photoprism
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}