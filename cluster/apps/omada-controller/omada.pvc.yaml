apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omada
  namespace: omada-controller
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-iscsi-csi
