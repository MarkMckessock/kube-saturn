apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: omada-data
  namespace: omada-controller
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}