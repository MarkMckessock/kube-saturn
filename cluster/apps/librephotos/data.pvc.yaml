apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: librephotos
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-nfs-csi
