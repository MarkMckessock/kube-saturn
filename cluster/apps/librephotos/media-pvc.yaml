apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: librephotos-media
  namespace: librephotos
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}