apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: media
  namespace: librephotos
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: truenas-nfs-csi
