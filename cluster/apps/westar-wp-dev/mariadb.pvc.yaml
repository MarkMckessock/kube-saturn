apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mariadb
  namespace: westar-wp-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: truenas-nfs-csi
