apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-data
  namespace: westar-wp-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ${STORAGE_CLASS}