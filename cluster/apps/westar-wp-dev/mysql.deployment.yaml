apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: westar-wp-dev
spec:
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8
        ports:
        - containerPort: 3306
          name: mysql
        volumeMounts:
          - mountPath: /var/lib/mysql
            name: mysql-data
            subPath: mysql
        envFrom:
          - secretRef:
              name: mysql-creds
      volumes:
        - name: mysql-data
          persistentVolumeClaim:
            claimName: mysql-data
