apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox
  namespace: netbox
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: truenas-nfs-csi
