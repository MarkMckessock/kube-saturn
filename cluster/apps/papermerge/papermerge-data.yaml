apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papermerge-data
  namespace: papermerge
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ${STORAGE_CLASS}