apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-data
  namespace: papermerge
spec:
  resources:
    requests:
      storage: 1Gi
  accessModes:
    - ReadWriteOnce
  storageClassName: ${STORAGE_CLASS}