apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: papermerge-media
  namespace: papermerge
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  storageClassName: ${STORAGE_CLASS}