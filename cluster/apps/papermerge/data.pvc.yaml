apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: data
  namespace: papermerge
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: truenas-iscsi-csi
