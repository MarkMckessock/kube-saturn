---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: sonarr-4k-secret
  namespace: media
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: sonarr-4k-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        SONARR__API_KEY: '{{ .api_key }}'
        SONARR__POSTGRES_HOST: postgres-rw.database.svc
        SONARR__POSTGRES_USER: '{{ .postgres_user }}'
        SONARR__POSTGRES_PASSWORD: '{{ .postgres_password }}'
        SONARR__POSTGRES_MAIN_DB: sonarr-4k
        SONARR__POSTGRES_LOG_DB: sonarr-4k-log
        POSTGRES_SUPERUSER_USER: '{{ .username }}'
        POSTGRES_SUPERUSER_PASS: '{{ .password }}'
  dataFrom:
    - extract:
        key: sonarr-4k
    - extract:
        key: cloudnative-pg-superuser

