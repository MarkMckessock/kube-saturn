---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media
resources:
  - ./helmrelease.yaml
  - ./backup
configMapGenerator:
  - name: youtube-dl-config
    files:
      - config/urls
generatorOptions:
  disableNameSuffixHash: true
