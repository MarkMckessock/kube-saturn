apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoview-photos
  namespace: photoview
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: ${STORAGE_CLASS}