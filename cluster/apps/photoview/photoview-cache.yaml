apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoview-cache
  namespace: photoview
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ${STORAGE_CLASS}